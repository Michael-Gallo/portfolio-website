baseURL = "https://michaelgallo.dev/"
title = "Michael Gallo"
enableRobotsTXT = false
disableKinds = ["taxonomy", "term"]
summaryLength = 100000

[params]
  description = "Michael Gallo is a senior software engineer building backend platforms and developer tools in Go and Python, including federated GraphQL on AWS."

[permalinks]
  blog = "/blog/:slugorcontentbasename"

[outputs]
  home = ["HTML", "RSS"]
  section = ["HTML"]
  page = ["HTML"]
